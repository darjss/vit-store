---



import type { ProductForHome } from "@/lib/types";
import ServerProductCard from "./server-product-card.astro";

interface Props {
	aspectRatio?: string;
	class?: string;
	imageSize?: "feature" | "card";
	product: ProductForHome;
}

const { aspectRatio, class: className, imageSize, product } = Astro.props;
---

<ServerProductCard
	product={{
		id: product.id,
		name: product.name,
		name_mn: product.nameMn,
		potency: product.potency,
		amount: product.amount,
		slug: product.slug,
		price: product.price,
		brand: product.brand ? { name: product.brand } : null,
		images: [{ url: product.image }],
		stock: product.stock,
		discount: product.discount,
	}}
	aspectRatio={aspectRatio}
	imageSize={imageSize}
	class={className}
/>
