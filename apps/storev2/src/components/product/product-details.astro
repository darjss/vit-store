---

import Tabs from "@/components/starwind/tabs/Tabs.astro";
import TabsContent from "@/components/starwind/tabs/TabsContent.astro";
import TabsList from "@/components/starwind/tabs/TabsList.astro";
import TabsTrigger from "@/components/starwind/tabs/TabsTrigger.astro";

interface Props {
	product: {
		description?: string | null;
		ingredients?: string[] | null;
		dailyIntake?: number | null;
		weightGrams?: number | null;
		amount?: string | null;
		potency?: string | null;
	};
}

const { product } = Astro.props;

const hasDescription = !!(
	product.description && product.description.trim().length > 0
);
const hasIngredients = Boolean(
	product.ingredients &&
	Array.isArray(product.ingredients) &&
	product.ingredients.length > 0
);
const hasSpecs = !!(
	product.dailyIntake ||
	product.weightGrams ||
	product.amount ||
	product.potency
);
console.log("ingredeients","has ingredients" ,product.ingredients, hasIngredients, product.ingredients?.length, typeof product.ingredients)
---

<div class="w-full space-y-6">
	<div class="border-t-4 border-black pt-6">
		<h2 class="text-2xl sm:text-3xl font-black mb-4">📋 Дэлгэрэнгүй мэдээлэл</h2>
	</div>

	<Tabs defaultValue="description" class="w-full">
		<TabsList class="w-full grid grid-cols-4 gap-2 p-2 bg-muted/50 border-3 border-black rounded-sm shadow-[4px_4px_0_0_#000] mb-6">
			<TabsTrigger 
				value="description" 
				class="text-xs sm:text-base font-black px-2 py-3 sm:px-4 sm:py-3 rounded-sm transition-all data-[state=active]:bg-primary data-[state=active]:border-2 data-[state=active]:border-black data-[state=active]:shadow-[2px_2px_0_0_#000] hover:bg-primary/30"
			>
				<span class="block sm:inline">📝</span>
				<span class="hidden sm:inline ml-1">Тайлбар</span>
			</TabsTrigger>
			<TabsTrigger 
				value="usage" 
				class="text-xs sm:text-base font-black px-2 py-3 sm:px-4 sm:py-3 rounded-sm transition-all data-[state=active]:bg-primary data-[state=active]:border-2 data-[state=active]:border-black data-[state=active]:shadow-[2px_2px_0_0_#000] hover:bg-primary/30"
			>
				<span class="block sm:inline">💊</span>
				<span class="hidden sm:inline ml-1">Хэрэглээ</span>
			</TabsTrigger>
			<TabsTrigger 
				value="ingredients" 
				class="text-xs sm:text-base font-black px-2 py-3 sm:px-4 sm:py-3 rounded-sm transition-all data-[state=active]:bg-primary data-[state=active]:border-2 data-[state=active]:border-black data-[state=active]:shadow-[2px_2px_0_0_#000] hover:bg-primary/30"
			>
				<span class="block sm:inline">🧪</span>
				<span class="hidden sm:inline ml-1">Найрлага</span>
			</TabsTrigger>
			<TabsTrigger 
				value="specs" 
				class="text-xs sm:text-base font-black px-2 py-3 sm:px-4 sm:py-3 rounded-sm transition-all data-[state=active]:bg-primary data-[state=active]:border-2 data-[state=active]:border-black data-[state=active]:shadow-[2px_2px_0_0_#000] hover:bg-primary/30"
			>
				<span class="block sm:inline">📊</span>
				<span class="hidden sm:inline ml-1">Үзүүлэлт</span>
			</TabsTrigger>
		</TabsList>

		<TabsContent value="description">
			<div class="rounded-sm border-3 border-black bg-white p-5 sm:p-7 shadow-[4px_4px_0_0_#000] min-h-[200px]">
				{hasDescription ? (
					<div class="prose prose-sm sm:prose max-w-none">
						<p class="text-sm sm:text-base leading-relaxed text-black font-normal whitespace-pre-line">
							{product.description}
						</p>
					</div>
				) : (
					<div class="flex flex-col items-center justify-center py-16 text-center">
						<div class="text-5xl mb-4">📝</div>
						<p class="text-base sm:text-lg text-black/60 font-bold">
							Тайлбар одоогоор байхгүй байна
						</p>
					</div>
				)}
			</div>
		</TabsContent>

		<TabsContent value="usage">
			<div class="rounded-sm border-3 border-black bg-white p-4 sm:p-6 shadow-[4px_4px_0_0_#000]">
				<div class="space-y-6">
					<div class="space-y-3">
						<h3 class="text-lg sm:text-xl font-black flex items-center gap-2">
							<span>💊</span>
							<span>Тун болон хэрэглэх арга</span>
						</h3>
						<div class="rounded-sm border-2 border-black bg-primary/10 p-4">
							{product.dailyIntake ? (
								<div class="space-y-2">
									<p class="text-sm sm:text-base font-bold">
										Өдөрт: <span class="text-lg font-black text-primary">{product.dailyIntake}</span> ширхэг
									</p>
									<p class="text-xs sm:text-sm text-black/70">
										Хоолны дараа усаар хамт уух
									</p>
								</div>
							) : (
								<p class="text-sm text-black/60">
									Багцын заавар эсвэл эмчийн зааварчилгааг дагана уу
								</p>
							)}
						</div>
					</div>

					<div class="space-y-3">
						<h3 class="text-lg sm:text-xl font-black flex items-center gap-2">
							<span>⏰</span>
							<span>Хэзээ уух вэ?</span>
						</h3>
						<div class="grid gap-3">
							<div class="flex items-start gap-3 p-3 rounded-sm border-2 border-black bg-white">
								<span class="text-2xl">🌅</span>
								<div>
									<div class="font-bold text-sm sm:text-base">Өглөө</div>
									<div class="text-xs sm:text-sm text-black/70">Өглөөний цайны дараа</div>
								</div>
							</div>
							<div class="flex items-start gap-3 p-3 rounded-sm border-2 border-black bg-white">
								<span class="text-2xl">🌃</span>
								<div>
									<div class="font-bold text-sm sm:text-base">Орой</div>
									<div class="text-xs sm:text-sm text-black/70">Оройн хоолны дараа</div>
								</div>
							</div>
						</div>
					</div>

					<div class="rounded-sm border-2 border-black bg-yellow-100 p-4">
						<h4 class="font-black text-sm sm:text-base mb-2 flex items-center gap-2">
							<span>⚠️</span>
							<span>Анхаарах зүйлс</span>
						</h4>
						<ul class="space-y-2 text-xs sm:text-sm text-black/80">
							<li class="flex gap-2">
								<span>•</span>
								<span>Зааврын дагуу хэрэглэнэ үү</span>
							</li>
							<li class="flex gap-2">
								<span>•</span>
								<span>Жирэмсэн, хөхүүл эх эмчтэй зөвлөлдөнө үү</span>
							</li>
							<li class="flex gap-2">
								<span>•</span>
								<span>Хадгалах: Сэрүүн, хуурай газарт</span>
							</li>
						</ul>
					</div>
				</div>
			</div>
		</TabsContent>

		<TabsContent value="ingredients">
			<div class="rounded-sm border-3 border-black bg-white p-4 sm:p-6 shadow-[4px_4px_0_0_#000] min-h-[200px]">
				{hasIngredients ? (
					<div class="space-y-4">
						<h3 class="text-lg sm:text-xl font-black mb-4">Идэвхтэй найрлага</h3>
						<ul class="grid gap-2">
							{product.ingredients?.map((ingredient) => (
								<li class="flex items-center gap-3 p-3 rounded-sm border-2 border-black bg-muted/30">
									<span class="text-lg">✓</span>
									<span class="text-sm sm:text-base font-semibold">{ingredient}</span>
								</li>
							))}
						</ul>
					</div>
				) : (
					<div class="flex flex-col items-center justify-center py-12 text-center">
						<div class="text-4xl mb-3">🧪</div>
						<p class="text-sm sm:text-base text-black/60 font-semibold">
							Найрлагын мэдээлэл одоогоор байхгүй байна
						</p>
					</div>
				)}
			</div>
		</TabsContent>

		<TabsContent value="specs">
			<div class="rounded-sm border-3 border-black bg-white p-4 sm:p-6 shadow-[4px_4px_0_0_#000] min-h-[200px]">
				{hasSpecs ? (
					<div class="space-y-4">
						<h3 class="text-lg sm:text-xl font-black mb-4">Техникийн үзүүлэлт</h3>
						<div class="grid gap-3">
							{product.amount && (
								<div class="flex items-center justify-between p-4 rounded-sm border-2 border-black bg-muted/30">
									<span class="text-sm sm:text-base font-bold text-black/70">⚖️ Тоо ширхэг</span>
									<span class="text-base sm:text-lg font-black">{product.amount}</span>
								</div>
							)}
							{product.potency && (
								<div class="flex items-center justify-between p-4 rounded-sm border-2 border-black bg-muted/30">
									<span class="text-sm sm:text-base font-bold text-black/70">💪 Идэвхтэй бодис</span>
									<span class="text-base sm:text-lg font-black">{product.potency}</span>
								</div>
							)}
							{product.dailyIntake && (
								<div class="flex items-center justify-between p-4 rounded-sm border-2 border-black bg-muted/30">
									<span class="text-sm sm:text-base font-bold text-black/70">📅 Өдрийн тун</span>
									<span class="text-base sm:text-lg font-black">{product.dailyIntake} ширхэг</span>
								</div>
							)}
							{product.weightGrams && (
								<div class="flex items-center justify-between p-4 rounded-sm border-2 border-black bg-muted/30">
									<span class="text-sm sm:text-base font-bold text-black/70">⚖️ Жин</span>
									<span class="text-base sm:text-lg font-black">{product.weightGrams}г</span>
								</div>
							)}
						</div>
					</div>
				) : (
					<div class="flex flex-col items-center justify-center py-12 text-center">
						<div class="text-4xl mb-3">📊</div>
						<p class="text-sm sm:text-base text-black/60 font-semibold">
							Техникийн үзүүлэлт одоогоор байхгүй байна
						</p>
					</div>
				)}
			</div>
		</TabsContent>
	</Tabs>

	{/* Disclaimer */}
	<div class="rounded-sm border-3 border-black bg-red-50 p-4 sm:p-6 shadow-[4px_4px_0_0_#000]">
		<p class="text-xs sm:text-sm font-bold text-center text-red-900 flex items-center justify-center gap-2 flex-wrap">
			<span class="text-base">⚠️</span>
			<span>Эмнэлгийн зөвлөгөө биш. Мэргэжлийн эмчид хандана уу.</span>
		</p>
	</div>
</div>
