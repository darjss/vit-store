# Customer-facing Flue Messenger agent

We will build the first Flue integration as a customer-facing Messenger agent rather than an admin assistant. The existing Messenger integration remains an admin alert surface, while the new Flue app owns inbound customer Messenger events on the test Page. This validates the real customer ordering flow first and leaves the future admin Messenger agent as a separate, later capability.
