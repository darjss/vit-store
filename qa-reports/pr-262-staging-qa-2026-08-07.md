# PR #262 Staging QA Report

Date: 2026-08-07  
PR: https://github.com/darjss/vit-store/pull/262  
Storefront: https://staging.amerikvitamin.mn  
Admin: https://admin-staging.amerikvitamin.mn  
API: https://api-staging.amerikvitamin.mn

## Goal

Verify that customers no longer choose a delivery zone during checkout and that admin staff choose the zone when sending orders to TU.

## Result

Partial pass.

- The storefront behavior passed.
- A clean staging order succeeded.
- The admin Send dialogs could not be tested because the staging dashboard had no app session and its Google OAuth callback still targeted the production API.
- The created QA order was removed.

Do not treat the admin part of PR #262 as verified from this run.

## Storefront

| Check | Result | Evidence |
| --- | --- | --- |
| Add a product to cart | Pass | Added one current in-stock product in the T3 browser. |
| Open checkout | Pass | Checkout loaded at `/checkout`. |
| Phone field | Pass | `input[name="phoneNumber"]` was present. |
| Address field | Pass | `textarea[name="address"]` was present. |
| Optional notes field | Pass | `textarea[name="notes"]` was present and labelled optional. |
| Submit control | Pass | The checkout form had a submit button. |
| No delivery-zone field | Pass | No zone input or selector appeared in the rendered form. |
| No zone loading or error state | Pass | No zone-specific loading state or error appeared. |
| No `getDeliveryAddressZones` request | Pass | Browser resource entries contained no request whose URL matched `getDeliveryAddressZones` or a delivery-zone route. |
| Create disposable staging order | Pass | `POST /trpc/store/order.addOrder` returned HTTP 200 and the UI showed order creation success. |
| Confirm `addressZoneId: null` | Not directly verified | The browser omitted the field and the deployed server accepts the omitted value, but this run did not read the stored row before cleanup. |

### Order evidence

The successful request used one current product and clear QA text. Worker tailing showed:

- procedure: `order.addOrder`
- outcome: success
- HTTP status: 200
- duration: about 1.78 seconds
- internal order ID: `524`
- order number: `R7ZA7VGQ`
- item count: 1
- status: pending

No payment was made.

### Earlier transient failure

An earlier attempt failed before Worker tailing started. That attempt used three cart items, including two old items already stored in the browser. The storefront showed its generic order-creation error. A later inventory refresh also showed an intermittent staging warning.

The exact cause cannot be proved because no historical staging logs were available. The clean one-item retry succeeded. The two likely causes are stale cart data or a short staging service failure. This was not a delivery-zone failure: no zone request occurred.

## Admin single-order test

Blocked; not tested.

The staging admin opened at `/login` and had no application session. The staging Google login endpoint declared this production callback:

```text
https://api.amerikvitamin.mn/admin/login/google/callback
```

The login flow was not started because the QA brief prohibited continuing through a production callback.

The following checks remain open:

- natural address appears in the Send dialog
- required delivery-zone selector appears
- Send starts disabled
- selecting a zone enables Send
- Cancel closes the dialog without dispatching the order

## Admin batch test

Blocked; not tested for the same login reason.

The following checks remain open:

- at least two pending orders can open the batch Send dialog
- each order has its own required zone selector
- Send stays disabled until every order has a zone
- distinct zones can be selected
- Send becomes enabled after all zones are selected
- Cancel closes the dialog without dispatching orders

No order was sent to TU and no customer order was changed.

## Worker tail findings

Worker tailing used `server-api-staging` and confirmed the deployed staging Worker handled the successful checkout.

### Post-creation Messenger error

After the order committed, the admin Messenger notification failed because Meta rejected a message outside the allowed messaging window. The server caught this error and still returned HTTP 200. It did not fail checkout or roll back the order.

### Staging QPay callback points to production

The staging Worker created the QPay invoice with a webhook callback under:

```text
https://api.amerikvitamin.mn/webhooks/qpay
```

This is the production API host. No payment was made during QA. Treat this as a separate staging-isolation issue.

### Log hygiene

Raw Worker tail events contained customer and payment fields. This report omits them. Do not attach the raw tail output to a PR or issue.

## Browser and console evidence

- T3 DOM inspection confirmed the expected checkout fields and the absence of a zone control.
- T3 resource inspection confirmed `order.addOrder` and no `getDeliveryAddressZones` request.
- The successful UI state showed `Захиалга амжилттай үүслээ`.
- No relevant browser console exception was captured.
- T3 snapshot calls failed on checkout and admin during this run, so there are no reliable checkout/admin screenshots to attach.
- Unrelated T3 warnings concerned WebMCP support and an `aria-hidden` focus warning.

## Cleanup

Cleanup passed.

- Soft-deleted QA order `R7ZA7VGQ` through the staging bot-auth admin route, which uses the same `deleteOrder` resolver as the dashboard.
- Verified deletion: `order.getOrderById({ id: 524 })` returned HTTP 404 with `Order not found`.
- Restored the browser's prior two-item cart.
- Confirmed browser storage contained no QA address or notes markers.
- Stopped Worker tailing.
- Closed every T3 browser tab created for this QA run.

## Follow-up needed

1. Obtain a staging admin application session without using the production OAuth callback.
2. Run both the single-order and batch Send dialog checks.
3. Directly verify that a newly created order stores `addressZoneId` as `null` before deleting it.
4. Fix or confirm the intended host for the staging QPay callback.
5. Investigate the intermittent inventory refresh warning if it recurs with a fresh cart.
